<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mustafa.smallstore">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App.SplashScreen">
        <activity
            android:name=".view.account.addandeditaccount.AddAndEditAccountActivity"
            android:exported="true"
            android:theme="@style/Theme.SmallStore" />
        <activity
            android:name=".view.login.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.SmallStore" />
        <activity
            android:name=".view.stepper.StepperLightActivity"
            android:exported="true"></activity>
        <activity
            android:name=".view.account.AccountActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>
        <activity
            android:name=".view.dashboard.MainActivity"
            android:exported="true" />
    </application>

</manifest>